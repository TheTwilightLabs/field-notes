# Teaching datasets

- `learners.csv` and `sales.csv` are original Field Notes teaching datasets, licensed CC BY 4.0.
- `iris.csv` is a compact local copy of R. A. Fisher's public-domain Iris dataset.

The datasets are intentionally small so beginners can inspect every row and notebooks remain fast.
