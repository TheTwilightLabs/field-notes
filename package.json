{
  "name": "field-notes-course-platform",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "generate:notebooks": "node scripts/generate-notebooks.mjs",
    "generate:game": "node scripts/generate-game.mjs",
    "verify:notebooks": "python3 scripts/verify-notebooks.py",
    "verify:course-data": "node scripts/verify-course-data.mjs",
    "verify:game": "python3 scripts/verify-game.py",
    "test": "npm run generate:notebooks && npm run generate:game && npm run verify:course-data && npm run verify:notebooks && npm run verify:game && npx tsc --noEmit"
  },
  "dependencies": {
    "next": "15.5.7",
    "react": "19.1.1",
    "react-dom": "19.1.1"
  },
  "devDependencies": {
    "@types/node": "22.15.30",
    "@types/react": "19.1.8",
    "@types/react-dom": "19.1.6",
    "typescript": "5.8.3"
  }
}
